name: Github Actions 실행해보기

# 실행되는 시점 설정: main branch에서 push가 실행될 때
on:
  push:
    branches:
      - main

# 실행 명령 설정
jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World 출력
        run: echo "Hello World"
